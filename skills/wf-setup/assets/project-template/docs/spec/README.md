# Specs

Use this directory for specs generated after a `/grill-me` or `/grill-with-docs` session.

Important: do not clear the conversation context before generating a spec. The spec should be extracted from the full grilling conversation.

Recommended filename:

```text
YYYY-MM-DD-feature-or-project-name.md
```
